1.0.3 -> 1.1 
	Missing values are handled without kNN imputation. The ROTS statistic is
	calculated without linear models to enable the easy handling of the
	missing values. Also, bootstrapping and permutation routines have been
	modified slightly so that they work with the missing values.
1.1 -> 1.1.1
	Improved memory comsumption.
1.1.1 -> 1.1.2
        Handling .C interface with new R release.

